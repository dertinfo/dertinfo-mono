name: Storage infra CD

# Deploys infra/bicep/storage at RG scope (images Storage Account always; SQL when
# prerequisitesExist is true in the Bicep param file).

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Deployment target'
        required: true
        type: choice
        default: full
        options:
          - full
          - dev-only
  push:
    branches:
      - main
    paths:
      - 'infra/bicep/storage/**'
      - '.github/workflows/storage-infra-cd.yml'
      - '.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml'

jobs:
  deploy-dev:
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.target == 'full' || inputs.target == 'dev-only'))
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: development
      resource_group: rg-dev-dertinfo-storage-uks
      template_path: infra/bicep/storage/main.bicep
      parameters_path: infra/bicep/storage/main.dev.bicepparam
      location: uksouth
      azure_oidc_workload: STORAGE
    secrets: inherit

  deploy-prod:
    needs: deploy-dev
    if: (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.target == 'full')) && needs.deploy-dev.result == 'success'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: production
      resource_group: rg-prd-dertinfo-storage-uks
      template_path: infra/bicep/storage/main.bicep
      parameters_path: infra/bicep/storage/main.prod.bicepparam
      location: uksouth
      azure_oidc_workload: STORAGE
    secrets: inherit
