name: Config infra CD

# Deploys infra/bicep/config at RG scope (Key Vault + App Configuration).

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Deployment target'
        required: true
        type: choice
        default: full
        options:
          - full
          - dev-only
  push:
    branches:
      - main
    paths:
      - 'infra/bicep/config/**'
      - '.github/workflows/config-infra-cd.yml'
      - '.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml'

jobs:
  deploy-dev:
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.target == 'full' || inputs.target == 'dev-only'))
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: development
      resource_group: rg-dev-dertinfo-config-uks
      template_path: infra/bicep/config/main.bicep
      parameters_path: infra/bicep/config/main.dev.bicepparam
      location: uksouth
      azure_oidc_workload: CONFIG
    secrets: inherit

  deploy-prod:
    needs: deploy-dev
    if: (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.target == 'full')) && needs.deploy-dev.result == 'success'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: production
      resource_group: rg-prd-dertinfo-config-uks
      template_path: infra/bicep/config/main.bicep
      parameters_path: infra/bicep/config/main.prod.bicepparam
      location: uksouth
      azure_oidc_workload: CONFIG
    secrets: inherit
