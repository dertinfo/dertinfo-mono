name: Monitoring infra CD

# Deploys infra/bicep/monitoring at RG scope (Log Analytics + Application Insights).

on:
  workflow_dispatch:
    inputs:
      target:
        description: 'Deployment target'
        required: true
        type: choice
        default: full
        options:
          - full
          - dev-only
  push:
    branches:
      - main
    paths:
      - 'infra/bicep/monitoring/**'
      - '.github/workflows/monitoring-infra-cd.yml'
      - '.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml'

jobs:
  deploy-dev:
    if: github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && (inputs.target == 'full' || inputs.target == 'dev-only'))
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: development
      resource_group: rg-dev-dertinfo-monitoring-uks
      template_path: infra/bicep/monitoring/main.bicep
      parameters_path: infra/bicep/monitoring/main.dev.bicepparam
      location: uksouth
    secrets: inherit

  deploy-prod:
    needs: deploy-dev
    if: (github.event_name == 'push' || (github.event_name == 'workflow_dispatch' && inputs.target == 'full')) && needs.deploy-dev.result == 'success'
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/reusable-infra-deploy-bicep-resourcegroup.yml
    with:
      environment: production
      resource_group: rg-prd-dertinfo-monitoring-uks
      template_path: infra/bicep/monitoring/main.bicep
      parameters_path: infra/bicep/monitoring/main.prod.bicepparam
      location: uksouth
    secrets: inherit
