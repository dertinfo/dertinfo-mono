# shared-contracts

API contract definitions shared between backend and frontends.

Typical contents after setup:
- OpenAPI spec (exported from or synced with `apps/dert-api/`)
- Generated TypeScript clients for `dert-web` and `dert-app`

Populate this package after the API is imported.
