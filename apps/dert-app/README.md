# dert-app

Ionic + Angular PWA.

This folder will be populated by importing the existing app repository with git history preserved.

After import, ensure `package.json` in this folder is registered as an npm workspace (already configured at repo root).
