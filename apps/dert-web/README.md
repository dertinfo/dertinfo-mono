# dert-web

Angular website.

This folder will be populated by importing the existing web repository with git history preserved.

After import, ensure `package.json` in this folder is registered as an npm workspace (already configured at repo root).
