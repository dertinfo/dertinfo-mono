# dert-api

.NET 8 Web API with EF Core and MSSQL.

This folder will be populated by importing the existing API repository with git history preserved.

After import, add the project to `dertinfo.sln` at the repo root if not already included.
