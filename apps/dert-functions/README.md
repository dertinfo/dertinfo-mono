# dert-functions

C# Azure Functions app (image resize).

This folder will be populated by importing the existing Functions repository with git history preserved.

After import, add the project to `dertinfo.sln` at the repo root.
