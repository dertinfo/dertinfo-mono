---
description: DertInfo monorepo architecture and cross-cutting rules
alwaysApply: true
---

# DertInfo Monorepo

## Apps
- **API**: `apps/dert-api/` — .NET 8, EF Core, MSSQL (migrations live here)
- **Functions**: `apps/dert-functions/` — C# Azure Functions (image resize)
- **Website**: `apps/dert-web/` — Angular SPA
- **App**: `apps/dert-app/` — Ionic + Angular PWA

## Packages
- **Shared contracts**: `packages/shared-contracts/` — OpenAPI spec and generated TS clients

## Workflow
1. Full-stack features: API first → shared contracts → frontend(s)
2. Check cwd before running `dotnet` or `npm` commands
3. Search for files before guessing paths; do not invent filenames
4. Follow gitflow: branch + PR to `main`/`develop`; never push directly to protected branches

## Boundaries
- Web and app do not share UI; no shared Angular library unless explicitly requested
- Do not move EF migrations out of the API

## Documentation
- Hub: `docs/README.md` — capabilities / technical / operations ([guide](docs/documentation-guide.md))

## Configuration
- See `docs/technical/infra/configuration.md` and `.cursor/rules/configuration.mdc`
- Checked-in config = local-working defaults; secrets in gitignored `infra/secrets/` (App Configuration when `AZURE_APP_CONFIG` is set)
- Local data under `infra/data/`; primary local path is native (SQL Server + Azurite + Auth0 via secrets)

## CI/CD
- See `docs/technical/infra/cicd.md` and `.cursor/rules/cicd.mdc`
- GitHub Actions: use **variables** for non-secret config; **secrets** only for tokens and credentials

## Change logs
- See `docs/operations/changelogs/README.md` and `.cursor/rules/changelogs.mdc`
- When a chat/workstream is completed: add an index row and a detail page under `docs/operations/changelogs/`
